import React from 'react';
import TextEditor from '../components/Editor/TextEditor';

const EditorPage = () => (
  <div>
    <h2>Editor Page</h2>
    <TextEditor />
  </div>
);

export default EditorPage;
